WORKFLOW — КАК РАБОТАТЬ С AGENTОМ ДЛЯ ЭКОНОМИИ ТОКЕНОВ
Зачем это нужно?
Вы платите за токены. Каждый раз, когда я читаю файлы из репозитория, я трачу токены. Эта документация — это "шпаргалка", которая позволит мне быстро понять контекст без дополнительных запросов к коду.

🎯 ЗОЛОТОЕ ПРАВИЛО
Пишите мне так, чтобы я мог ответить, не читая код из репозитория.

📝 ШАБЛОН ЗАПРОСА (экономный способ)
❌ ПЛОХО (много токенов):
Мне нужно изменить функцию проверки аффикса.

Почему плохо?
Я не знаю:

Какую функцию (может быть несколько)
Что менять (добавить? удалить? исправить?)
Где она находится (какой файл?)
Результат: Я прочитаю несколько файлов, потрачу много токенов.

✅ ХОРОШО (мало токенов):
Code
Файл: Services/AffixMatch.cs, метод IsMatch()

Сейчас: Проверяет, содержится ли текст в буфере (подстрока)

Нужно: Добавить поддержку регулярных выражений

Детали: 
- Если шаблон начинается с / и заканчивается на / → это regex
- Например: /Fire\s+(\d+)/ → ищет "Fire 42"
- Число сравнить с минимальным порогом
Почему хорошо?
Я всё сразу понял:

Какой файл
Какой метод
Что менять
Как менять
Результат: Я могу ответить, не читая код!

🗂️ СТРУКТУРА МОЕГО КОНТЕКСТА
Я уже знаю эту информацию из документов docs/:

✅ Я ЗНАЮ:
Структура проекта (файлы и папки)
Назначение каждого класса/файла
Как работает цикл крафта
Как сохраняются настройки
Как логируются данные
Как работает выделение областей
❌ Я НЕ ЗНАЮ (пока не прочитаю код):
Точные переменные в коде
Точные методы и их сигнатуры
Конкретные значения и констант
Текущие баги или проблемы
💡 ПРИМЕРЫ ЗАПРОСОВ
Пример 1: Добавить новую фичу
Code
🎯 ЗАДАЧА: Добавить поддержку сохранения истории крафтов

📁 ФАЙЛЫ:
  - Services/ChaosCraftService.cs → метод StartCraftAsync()
  - AppSettings.cs → добавить список крафтов

📝 ОПИСАНИЕ:
Сейчас: После крафта результат теряется
Нужно: Сохранять каждый результат в список в памяти

ДЕТАЛИ:
  - Добавить в AppSettings: List<CraftHistoryItem> History
  - CraftHistoryItem должен содержать:
    * Дата/время крафта
    * Результат (SUCCESS/FAILED)
    * Количество попыток
    * Использованный шаблон
  - В StartCraftAsync() добавить добавление в History
  - При загрузке приложения: очищать History (новая сессия)

ВОПРОС: Нужна ли сохранение History в settings.json, или только в памяти?
Результат:
Я знаю:

Где менять ✓
Что именно менять ✓
Почему менять ✓
Я могу написать код без дополнительных чтений ✓
Пример 2: Исправить баг
Code
🐛 БАГ: При запуске крафта интерфейс зависает на 5 секунд

📁 ФАЙЛ: MainWindow.xaml.cs, обработчик btnStartCraft_Click()

📝 ОПИСАНИЕ:
Проблема: Вызов StartCraftAsync() не асинхронный, поэтому UI зависает

Почему: Код выглядит примерно так (предположу):
  var result = chaosCraftService.StartCraftAsync(settings);

А должно быть асинхронно:
  var result = await chaosCraftService.StartCraftAsync(settings);

ВОПРОС: Нужно ли использовать Task.Run() или достаточно await?
Результат:
Я знаю проблему и предложу решение без чтения файла.

Пример 3: Оптимизация
Code
⚡ ОПТИМИЗАЦИЯ: Сбросить координаты областей на безопасные значения

📁 ФАЙЛ: ProjectPaths.cs (вспомогательная функция)

📝 ОПИСАНИЕ:
Сейчас: При запуске приложения области не проверяются на валидность
Нужно: Проверить, что области видны на экранах пользователя

ДЕТАЛИ:
  - Если X, Y выходят за границы экрана → установить на (100, 100)
  - Если Width или Height = 0 → установить на (50, 50)
  - При загрузке AppSettings вызовать эту проверку

ВОПРОС: Как получить информацию о всех мониторах?
📊 ЧЕКЛИСТ: ИДЕАЛЬНЫЙ ЗАПРОС
Когда вы пишете мне запрос, проверьте:

Code
☐ Написал, какой файл нужно менять?
☐ Написал, какой метод/класс нужно менять?
☐ Написал, что сейчас и что нужно?
☐ Написал, почему это нужно?
☐ Если есть код-пример, вставил его?
☐ Если не уверен в деталях, написал вопрос?
🚀 КОГДА Я ПОТРАЧУ МНОГО ТОКЕНОВ (и это OK)
Есть ситуации, когда мне нужно прочитать код:

Читаю код в первый раз
Например: "Покажи, как устроена проверка аффикса в AffixMatch.cs"
→ Я прочитаю файл полностью

Отладка и анализ
Например: "Почему не работает сохранение настроек?"
→ Я прочитаю SettingsStore.cs, AppSettings.cs, ProjectPaths.cs

Полная переработка функции
Например: "Переделай весь цикл крафта на многопоточность"
→ Я прочитаю ChaosCraftService.cs целиком

В этих случаях траты оправданы, потому что нужна полная информация.

📚 ДОКУМЕНТЫ, КОТОРЫЕ Я ВСЕГДА ЧИТАЮ
Эти документы я читаю автоматически при каждом разговоре:

Code
✓ docs/SRS.md → требования проекта
✓ docs/ROADMAP.md → текущие цели
✓ docs/STRUCTURE.md → структура файлов (этот новый документ!)
Поэтому в запросе можно ссылаться на них по ID:

Code
"Согласно SRS.md, FR-005, режим с n должен..."
"Как указано в ROADMAP.md, текущая цель — стабилизация тайминов"
🎯 ИТОГОВЫЙ АЛГОРИТМ
Когда вы пишете запрос:

Code
1. Опишите, что нужно, не читая код:
   "Мне нужно добавить новый параметр в AppSettings"

2. Укажите, где:
   "AppSettings.cs, класс AppSettings"

3. Укажите, что и почему:
   "Добавить int DelayBetweenClicks, потому что тайминги нужны"

4. Если не уверены, спросите:
   "Нужно ли это сохранять в settings.json?"

5. Я отвечу БЕЗ чтения кода из репозитория
💰 ЭКОНОМИЯ ТОКЕНОВ
До этой документации:

Каждый запрос → я читаю 3-5 файлов → ~2000+ токенов
После этой документации:

Запрос → я понимаю контекст → 200-500 токенов
Сэкономим:

На 10 запросов: ~15000 токенов 🎉
